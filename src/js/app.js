(function($, Drupal) {
    Drupal.behaviors.slickNav = {
        attach: function(context, settings) {
            // $('#slicknav .menu').slicknav({
            //     prependTo: '#main'
            // });
        }
    };
})(jQuery, Drupal);